```
    This is a hello-docker image i just created to display message on the terminal with js, A simple docker project published to the docker hub and private repository Amazon Elastic Container Registry readily available to be pulled and run in a docker container
```
