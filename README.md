```
    This is a hello-docker image i just created, A simple docker project published to the docker hub and private repository Amazon Elastic Container Registry readily available to be pulled and run in a docker container
```
